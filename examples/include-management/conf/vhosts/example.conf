server {
    listen 80;
    server_name example.com;
    root /var/www/example.com;
    location / {
        try_files $uri $uri/ =404;
    }
    location /api {
        proxy_pass http://backend;
    }
    location /health {
        return 200 'OK';
    }
}